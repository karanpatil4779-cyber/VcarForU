# MySQL Database URL (Railway, PlanetScale, or other MySQL provider)
# For local: mysql://root:password@localhost:3306/vcarforu
# For Railway: mysql://username:password@hostname:port/database
DATABASE_URL=""

# Frontend API URL (only needed for local dev if APIs are on different port)
# For production on Vercel, leave empty as /api routes work automatically
VITE_API_URL=""